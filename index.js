// misc

let textSize = function(el, fontSize) {
	el.style.fontSize = fontSize
	var height = (el.offsetHeight + 1)
	var width = (el.offsetWidth + 1)
	return [width, height]
}

let link = [
	{
		caption: 'FEFU Invaders',
	},
	{
		caption: 'WebGL testing'
	}
]

let fontSize = 18
let id = 0

window.onload = function() {

}